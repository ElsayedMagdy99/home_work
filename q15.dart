/*What does it mean for Dart to be a statically-typed language, and how does it
impact the way variables are used? */
void main() {
  //Dart to be a statically-typed language: it's meaning , types are checked at compile time
  //example
  String name = 'ali';
 // name = 123;
  //Discover type-related bugs at compile time.
  /*if the data type of variables is known at compile time. 
   its main advantage is that The compiler can quickly check the issues and detect bugs*/
   
}
